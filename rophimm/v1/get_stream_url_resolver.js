function getStreamUrl(episodeId){try{let id=episodeId;if(typeof episodeId==='string'&&episodeId.startsWith('http')){const parts=episodeId.split('/').pop().split('-');const lastPart=parts[parts.length-1];if(/^\d+$/.test(lastPart)){id=lastPart;}else{id=episodeId.split('/').pop();}}const embedUrl='https:console.log('[JS] Stream URL(embed):'+embedUrl);return JSON.stringify({type:'embed',url:embedUrl,headers:{'Referer':'https:});}catch(e){return JSON.stringify({error:e.message});}}